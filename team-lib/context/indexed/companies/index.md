# companies/

One folder per company/brand whose context the team works with: `companies/<name>/`.

Typical contents: `brand/` (guidelines, tokens, assets, templates — see the
`create-brand-guidelines` and `compose-branded-template` skills), plus indexed
business-context files. This reference repo ships the convention empty; your
own company folders are created by the brand skills on first use.
