---
name: example-project
description: Demonstrates the project memory type — active work context
metadata:
  type: project
---

Example project is targeting a March 30 milestone for the API redesign.

**Why:** The redesign was driven by performance requirements — the current sync API can't handle the projected load increase.

**How to apply:** Scope decisions for the API layer should favor performance over developer ergonomics until the milestone is met.
