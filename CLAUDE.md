# STOP - WRONG DIRECTORY

You are at the **Workspace Root**. No work is permitted here.

This directory is the top-level container for the AI Workspace. All work happens in one of the four layers:

- **personal/** — Local overrides and secrets (Layer 0)
- **team-lib/** — Shared standard library (Layer 1)
- **my-lib/** — Personal extensions (Layer 2)
- **projects/** — Active development (Layer 3)

Route to the appropriate layer before proceeding.
